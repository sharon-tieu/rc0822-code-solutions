/* exported getFirstChar */

// write a function that gets the first character of a string- this is at index 0
// return the first letter of a string which is at index 0

function getFirstChar(string) {
  return string[0];
}
