/* exported getLastChar */

// write a function that will return the last letter of a string
// the last letter of a string lies on the last index.
//   this will look like string[string.length - 1];

function getLastChar(string) {
  return string[string.length - 1];
}
