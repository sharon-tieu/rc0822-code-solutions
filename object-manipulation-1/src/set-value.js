/* exported setValue */
// write a function that will "not return anything", meaning to return whatever is already the object literal?
function setValue(object, key, value) {
  object[key] = value;
}
