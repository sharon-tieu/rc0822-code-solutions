/* exported getValue */

// write a function that will return the key property found in an object
// access the key in property by using bracket notation
//     object[key]
// return the object at key

function getValue(object, key) {
  return object[key];
}
