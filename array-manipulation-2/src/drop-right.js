/* exported dropRight */
// write a function that will return the elements of the array that is after the given counts from the end of the array
// assign a new variable to an empty array
// special case:
//    if the count is larger than the length of the array, then return the original array
// loop through the array with a for loop
//    initialization: end of the array
//    condition: iterate through the array until it reaches the beginning so keep iterating when i is still greater than or equal to 0
//    final expression: decrement
// if i is less than count, then put element in new array
//    use .unshift() so then the most recent element will be placed in the beginning of the array
// break out of the loop
// else if ...
// if
